const ROUTES = {
  /* landing layout */
  ROOT: '/',
  HOME: '/home',
  THREADS: '/threads',
  EVENTS: '/events',
  CONTACT: '/contact',
  POST: '/post',
} as const;

const BACKEND_ROUTES = {} as const;

export { BACKEND_ROUTES, ROUTES };
