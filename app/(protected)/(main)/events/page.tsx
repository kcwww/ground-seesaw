import SIDEBAR from '@/constants/sidebar';

const EventsPage = () => {
  return (
    <section className={'md:ml-[' + SIDEBAR.SIZE + ']'}>
      <h1>Events Page</h1>
    </section>
  );
};

export default EventsPage;
